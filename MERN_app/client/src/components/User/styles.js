import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  loadingPaper: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    padding: "20px",
    borderRadius: "15px",
    height: "39vh",
  },
});
